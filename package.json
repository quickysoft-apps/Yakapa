{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "clean": "lerna clean --yes && rm -rf ./node_modules"
  }
}